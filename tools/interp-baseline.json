[
  "(Math.round(hrs * 100) / 100)",
  "(act ? rPct : pPct).toFixed(1)",
  "(act.secs / 3600).toFixed(1)",
  "(ad * 100).toFixed(0)",
  "(d.meals||[]).map(ml => `<div class=\"pmeal${/on the bike/i.test(ml.l||'')?' onbike':''}\"> <div class=\"pt\">${esc(ml.t)}</div> <div class=\"px\"><b>${esc(ml.l)}</b>${combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')}<span class=\"pm\">${Number(ml.kc)||0} kcal · ${Number(ml.cb)||0} g carb</span></div> </div>`).join('')",
  "/on the bike/i.test(ml.l||'')?' onbike':''",
  "DOW[d.getDay()]",
  "Math.round(6 * RIDER_KG)",
  "Math.round(8 * RIDER_KG)",
  "Math.round(FLAT_HI * RIDER_KG)",
  "Math.round(FLAT_LO * RIDER_KG)",
  "Math.round(aiFood.food.per100.kc * aiFood.food.unit.g / 100)",
  "Math.round(mac.c)",
  "Math.round(mac.f)",
  "Math.round(mac.kc)",
  "Math.round(mac.p)",
  "Math.round(total)",
  "Math.round(tr.h * 600)",
  "Number(bk.cb) || 0",
  "Number(bk.kc) || 0",
  "Number(c.kcal_delta)||0",
  "Number(d.cb)||0",
  "Number(d.d)||0",
  "Number(d.kc)||0",
  "Number(day.d) || 0",
  "Number(dn)",
  "Number(it.c) || 0",
  "Number(it.c)||0",
  "Number(m.cb) || 0",
  "Number(m.kc) || 0",
  "Number(ml.cb)||0",
  "Number(ml.kc)||0",
  "WD.map(d => `<div class=\"cghead\">${esc(d)}</div>`).join('')",
  "act",
  "act && !act.meas ? ' est' : ''",
  "act ? ' s' + rideStep(kph) : ''",
  "aiFood.busy ? ' disabled' : ''",
  "aiFood.food.unit.g",
  "at >= 0 ? clock(at + 1) : '&mdash;'",
  "bPct.toFixed(1)",
  "bk ? `<span class=\"pi\"><i>${Number(bk.cb) || 0}g</i>carb on the bike &middot; ${Number(bk.kc) || 0} kcal</span>` : ''",
  "bkc",
  "bkk",
  "burn",
  "c.kcal_delta < 0 ? 'dn' : 'up'",
  "c.kcal_delta > 0 ? '+' : ''",
  "cells.join('')",
  "clock(mins(m.t))",
  "cls ? ' ' + cls : ''",
  "cls ? '*' : ''",
  "coach.busy ? ' disabled' : ''",
  "coach.busy ? 'Thinking&hellip;' : 'What should change?'",
  "coach.rides === null ? '&hellip;' : 'nothing yet'",
  "code.k",
  "combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')",
  "cook?' cook':''",
  "d.cook?' cook':''",
  "d.getDate()",
  "d.h ? ' · ' + (Number(d.h)||0) + ' h' : ''",
  "d.items.length",
  "day.meals.length",
  "dayRide.busy ? ' disabled' : ''",
  "dayRide.busy ? 'Reading it&hellip;' : (dayRide.read ? 'Read it again' : 'What was this ride?')",
  "dcb",
  "dkc",
  "dn.c ? '$' + (Number(dn.c) || 0).toFixed(2) : ''",
  "esc(a.caveat)",
  "esc(a.detail)",
  "esc(a.headline)",
  "esc(aiFood.err)",
  "esc(aiFood.food.n)",
  "esc(aiFood.food.unit.u)",
  "esc(big.name || big.type)",
  "esc(build.name || '')",
  "esc(c.change)",
  "esc(c.meal)",
  "esc(coach.err)",
  "esc(code.name)",
  "esc(code.why)",
  "esc(d)",
  "esc(d.items.map(i => i.q + ' ' + i.u + ' ' + i.f).join(', '))",
  "esc(d.kind)",
  "esc(d.name || 'Untitled')",
  "esc(d.wd)",
  "esc(day.kind)",
  "esc(day.wd)",
  "esc(dayRide.err)",
  "esc(dn.d)",
  "esc(dn.dish)",
  "esc(dn.tag || 'cook tonight')",
  "esc(f[0])",
  "esc(fq)",
  "esc(fq.trim())",
  "esc(hh)",
  "esc(hhm(((tr&&tr.h)||0)))",
  "esc(hhm(secs/3600))",
  "esc(hhm(tr.h))",
  "esc(iso)",
  "esc(it.extraId)",
  "esc(it.f)",
  "esc(it.n)",
  "esc(it.note)",
  "esc(it.q || '')",
  "esc(it.q)",
  "esc(it.u)",
  "esc(k)",
  "esc(key)",
  "esc(label)",
  "esc(look.err)",
  "esc(m.l)",
  "esc(m.t)",
  "esc(ml.l)",
  "esc(ml.t)",
  "esc(pp)",
  "esc(r.serves)",
  "esc(s)",
  "esc(s.sec)",
  "esc(selDay)",
  "esc(t.kind || d.kind || 'Ride')",
  "esc(tr.kind)",
  "esc(u[0])",
  "esc(w.note)",
  "esc(w.week.slice(5))",
  "esc(x)",
  "esc(x.body)",
  "esc(x.title)",
  "f > 0 ? 'true' : 'false'",
  "from ? ' <i>from ' + esc(from.d) + '</i>' : ''",
  "gap",
  "gap < 0 ? 'under' : 'over'",
  "gap > 0 ? '+' : ''",
  "gph",
  "hPct.toFixed(1)",
  "has ? '' : ' off'",
  "heats.length ? ' &middot; reheats ' + esc(heats.join(', ')) : ''",
  "hh",
  "hideChecked ? 'All items checked off!' : 'Nothing from ' + (tab==='M'?'Meijer':'Aldi') + ' this week.'",
  "hrs.toFixed(1)",
  "i",
  "ings.length ? `<div class=\"rsec\">What you need</div> <ul class=\"ring\">${ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')}</ul>` : ''",
  "ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')",
  "isNow ? ' today' : ''",
  "isToday ? ' istoday' : ''",
  "isToday ? '<div class=\"todaytag\">Today</div>' : ''",
  "it.c > 0 ? it.c.toFixed(2) : ''",
  "it.extraId ? `<button class=\"del\" data-del=\"${esc(it.extraId)}\" aria-label=\"Remove ${esc(it.n)}\">\\u00d7</button>` : ''",
  "it.note ? `<span class=\"note\">${esc(it.note)}</span>`:''",
  "it.pantry === 'out' ? 'Out' : 'Low'",
  "it.pantry ? ` data-pantry-name=\"${esc(it.n)}\"` : ''",
  "it.pantry ? `<span class=\"tagP\">${it.pantry === 'out' ? 'Out' : 'Low'}</span>` : ''",
  "it.q ? `<span class=\"q\">${esc(it.q)}</span>`:''",
  "it.store === 'M' ? ' <b class=\"tagM\">Meijer</b>' : ''",
  "it.store === 'M' ? '<b>Meijer</b>' : ''",
  "items",
  "k",
  "key === selDay ? ' on' : ''",
  "key === todayKey() ? ' now' : ''",
  "l",
  "look.busy ? ' disabled' : ''",
  "look.busy ? 'Reading the data&hellip;' : (look.data ? 'Look again' : 'Analyse my training')",
  "meas ? '<span class=\"meas\">power meter</span>' : ''",
  "on?' done':''",
  "onbike",
  "out",
  "pPct.toFixed(1)",
  "pct",
  "perHr",
  "perHr ? `<span class=\"pm\">about ${perHr} g carb an hour</span>` : ''",
  "planK",
  "r && r.pantry && r.pantry.length ? `<div class=\"rsec\">From the cupboard</div> <ul class=\"ring pantryring\">${r.pantry.map(x => `<li>${esc(x)}</li>`).join('')}</ul>` : ''",
  "r ? `<div class=\"rsec\">Method</div> <ol class=\"rsteps\">${r.method.map(s => `<li>${esc(s)}</li>`).join('')}</ol> <p class=\"rtip\">${esc(r.serves)}</p>` : `<p class=\"rtip\">No method written for this one yet.</p>`",
  "r.method.map(s => `<li>${esc(s)}</li>`).join('')",
  "r.pantry.map(x => `<li>${esc(x)}</li>`).join('')",
  "ride",
  "rideState",
  "rides",
  "rs.length - 1",
  "rs.length === 2 ? 'ride' : 'rides'",
  "said",
  "sectionsHtml(dayRide.read)",
  "sectionsHtml(look.data)",
  "shopView === 'dinners' ? ' class=\"on\"' : ''",
  "shopView === 'list' ? ' class=\"on\"' : ''",
  "st",
  "st.efficiency_trend_pct",
  "st.efficiency_trend_pct < 0 ? 'dn' : 'up'",
  "st.efficiency_trend_pct > 0 ? '+' : ''",
  "sum.cb",
  "sum.kc",
  "t",
  "t ? Math.min(100, Math.round(v / t * 100)) : 0",
  "tr && tr.h ? esc(hhm(tr.h)) + ' planned' : 'no riding'",
  "u",
  "u ? Math.round(u[2] * (Number(it.q)||0)) : 0",
  "u[2]",
  "v",
  "w.hours",
  "w.partial ? ' part' : ''",
  "w.ride_days",
  "w.watts_per_bpm || '&mdash;'"
]
