[
  "(Math.round(hrs * 100) / 100)",
  "(d.meals||[]).map(ml => `<div class=\"pmeal${/on the bike/i.test(ml.l||'')?' onbike':''}\"> <div class=\"pt\">${esc(ml.t)}</div> <div class=\"px\"><b>${esc(ml.l)}</b>${(ml.i||[]).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')}<span class=\"pm\">${Number(ml.kc)||0} kcal · ${Number(ml.cb)||0} g carb</span></div> </div>`).join('')",
  "(ml.i||[]).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')",
  "/on the bike/i.test(ml.l||'')?' onbike':''",
  "Number(d.cb)||0",
  "Number(d.d)||0",
  "Number(d.kc)||0",
  "Number(it.c) || 0",
  "Number(it.c)||0",
  "Number(ml.cb)||0",
  "Number(ml.kc)||0",
  "Number(t.bk.cb) || 0",
  "Number(t.bk.kc) || 0",
  "Number(t.d) || 0",
  "Number(t.h) || 0",
  "Number(x.d) || 0",
  "WD.map(d => `<div class=\"mhead\">${esc(d)}</div>`).join('')",
  "cells.join('')",
  "cook?' cook':''",
  "d.cook?' cook':''",
  "d.h ? ' · ' + (Number(d.h)||0) + ' h' : ''",
  "dn.c ? '$' + (Number(dn.c) || 0).toFixed(2) : ''",
  "esc(d)",
  "esc(d.kind)",
  "esc(d.wd)",
  "esc(dn.d)",
  "esc(dn.dish)",
  "esc(dn.tag || 'cook tonight')",
  "esc(hh)",
  "esc(it.extraId)",
  "esc(it.n)",
  "esc(it.note)",
  "esc(it.q || '')",
  "esc(it.q)",
  "esc(k)",
  "esc(ml.l)",
  "esc(ml.t)",
  "esc(pp)",
  "esc(r.serves)",
  "esc(s)",
  "esc(s.sec)",
  "esc(t.kind || '')",
  "esc(t.wd)",
  "esc(w.note)",
  "esc(weekLabel(w))",
  "esc(x)",
  "from ? ' <i>from ' + esc(from.d) + '</i>' : ''",
  "h20",
  "heats.length ? ' &middot; reheats ' + esc(heats.join(', ')) : ''",
  "hideChecked ? 'All items checked off!' : 'Nothing from ' + (tab==='M'?'Meijer':'Aldi') + ' this week.'",
  "hrs > 0 ? hrs : '\\u2013'",
  "i",
  "i===wi?' on':''",
  "ings.length ? `<div class=\"rsec\">What you need</div> <ul class=\"ring\">${ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')}</ul>` : ''",
  "ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')",
  "it.c > 0 ? it.c.toFixed(2) : ''",
  "it.extraId ? `<button class=\"del\" data-del=\"${esc(it.extraId)}\" aria-label=\"Remove ${esc(it.n)}\">\\u00d7</button>` : ''",
  "it.note ? `<span class=\"note\">${esc(it.note)}</span>`:''",
  "it.pantry === 'out' ? 'Out' : 'Low'",
  "it.pantry ? ` data-pantry-name=\"${esc(it.n)}\"` : ''",
  "it.pantry ? `<span class=\"tagP\">${it.pantry === 'out' ? 'Out' : 'Low'}</span>` : ''",
  "it.q ? `<span class=\"q\">${esc(it.q)}</span>`:''",
  "it.store === 'M' ? ' <b class=\"tagM\">Meijer</b>' : ''",
  "it.store === 'M' ? '<b>Meijer</b>' : ''",
  "on?' done':''",
  "onbike",
  "r ? `<div class=\"rsec\">Method</div> <ol class=\"rsteps\">${r.method.map(s => `<li>${esc(s)}</li>`).join('')}</ol> <p class=\"rtip\">${esc(r.serves)}</p>` : `<p class=\"rtip\">No method written for this one yet.</p>`",
  "r.method.map(s => `<li>${esc(s)}</li>`).join('')",
  "rides",
  "riding ? '' : ' rest'",
  "riding ? 'no on-bike fuel needed' : 'rest'",
  "riding ? `<span class=\"thrs\">${Number(t.h) || 0} h</span>` : ''",
  "s",
  "t.bk ? `<b>${Number(t.bk.cb) || 0}<i>g carb</i></b><span>${Number(t.bk.kc) || 0} kcal on the bike</span>` : `<span>${riding ? 'no on-bike fuel needed' : 'rest'}</span>`",
  "wk.map(t => { const riding = (Number(t.h) || 0) > 0; const k = String(t.kind || '').toLowerCase().replace(/[^a-z]/g, ''); return `<div class=\"tday${riding ? '' : ' rest'}\"> <div class=\"tdate\"><b>${esc(t.wd)}</b><span>${Number(t.d) || 0}</span></div> <div class=\"tmid\"> <span class=\"tkind k-${esc(k)}\">${esc(t.kind || '')}</span> ${riding ? `<span class=\"thrs\">${Number(t.h) || 0} h</span>` : ''} </div> <div class=\"tfuel\">${t.bk ? `<b>${Number(t.bk.cb) || 0}<i>g carb</i></b><span>${Number(t.bk.kc) || 0} kcal on the bike</span>` : `<span>${riding ? 'no on-bike fuel needed' : 'rest'}</span>`}</div> </div>`; }).join('')"
]
