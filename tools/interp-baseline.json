[
  "(Math.round(hrs * 10) / 10)",
  "(act ? rPct : pPct).toFixed(1)",
  "(act.secs / 3600).toFixed(1)",
  "(ad * 100).toFixed(0)",
  "(d.meals||[]).map(ml => `<div class=\"pmeal${/on the bike/i.test(ml.l||'')?' onbike':''}\"> <div class=\"pt\">${esc(ml.t)}</div> <div class=\"px\"><b>${esc(ml.l)}</b>${combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')}<span class=\"pm\">${Number(ml.kc)||0} kcal · ${Number(ml.cb)||0} g carb</span></div> </div>`).join('')",
  "(m.tags || []).map(t => `<span class=\"rtag-pill\">#${esc(t)}</span>`).join('')",
  "(recipeSearch || recipeCategory !== 'ALL') ? `<button class=\"rec-reset-simple\" id=\"rec_reset_simple\">Reset</button>` : ''",
  "/on the bike/i.test(ml.l||'')?' onbike':''",
  "DOW[d.getDay()]",
  "FOODS.length",
  "FRAC[f] || ''",
  "FRAC[x.f] || ''",
  "Math.min(5, i+1)",
  "Math.round((r.cool||0)/r.secs*100)",
  "Math.round((r.warm||0)/r.secs*100)",
  "Math.round(6 * riderKg)",
  "Math.round(8 * riderKg)",
  "Math.round(FLAT_HI * riderKg)",
  "Math.round(FLAT_LO * riderKg)",
  "Math.round(aiFood.food.per100.kc * aiFood.food.unit.g / 100)",
  "Math.round(cb)",
  "Math.round(f.ctl)",
  "Math.round(f.tsb)",
  "Math.round(f[2] ? f[2][2] : 0)",
  "Math.round(f[2] ? f[2][3] : 0)",
  "Math.round(f[2] ? f[2][4] : 0)",
  "Math.round(f[2] ? f[2][5] : 0)",
  "Math.round(kc)",
  "Math.round(mac.c)",
  "Math.round(mac.f)",
  "Math.round(mac.kc)",
  "Math.round(mac.p)",
  "Math.round(sec/tot*100)",
  "Math.round(swap.kc)",
  "Math.round(total)",
  "Math.round(tr.h * 600)",
  "Math.round(work/r.secs*100)",
  "Math.round(x.cb)",
  "Math.round(x.kc)",
  "Number(bk.cb) || 0",
  "Number(bk.kc) || 0",
  "Number(c.kcal_delta)||0",
  "Number(cal) || 0",
  "Number(d.cb)||0",
  "Number(d.d)||0",
  "Number(d.kc)||0",
  "Number(day.d) || 0",
  "Number(dn)",
  "Number(it.c) || 0",
  "Number(it.c)||0",
  "Number(it.q) || 1",
  "Number(m.cb) || 0",
  "Number(m.kc) || 0",
  "Number(ml.cb)||0",
  "Number(ml.kc)||0",
  "QCLS[f] || ''",
  "QCLS[x.f] || ''",
  "S.zk",
  "SIMPLE_CATEGORIES.filter(c => c.id !== 'quick-prep' && c.id !== 'single-pan').map(c => `<button class=\"rec-cat-btn${recipeCategory === c.id ? ' on' : ''}\" data-rcat=\"${esc(c.id)}\">${esc(c.label)}</button>`).join('')",
  "WD.map(d => `<div class=\"cghead\">${esc(d)}</div>`).join('')",
  "act",
  "act && !act.meas ? ' est' : ''",
  "act ? ' ' + dayZone(day, act).k : ''",
  "act ? ' has-act' : ''",
  "addedMeals.length",
  "addedMeals.map(mName => ` <span class=\"sam-chip\"> <span>${esc(mName)}</span> <button class=\"sam-del-btn\" data-rrmshop=\"${esc(mName)}\" aria-label=\"Remove ${esc(mName)} from list\">&times;</button> </span> `).join('')",
  "aiFood.busy ? ' disabled' : ''",
  "aiFood.food.unit.g",
  "aisleIcon(s.sec)",
  "aisleIcon(sec)",
  "at >= 0 && at < 24 * 60 ? clock(at) : '&mdash;'",
  "at >= 0 ? clock(at + 1) : '&mdash;'",
  "bPct.toFixed(1)",
  "bk ? `<span class=\"pi\"><i>${Number(bk.cb) || 0}g</i>carb on the bike &middot; ${Number(bk.kc) || 0} kcal</span>` : ''",
  "bkc",
  "bkk",
  "burn",
  "c.kcal_delta < 0 ? 'dn' : 'up'",
  "c.kcal_delta > 0 ? '+' : ''",
  "cells.join('')",
  "clock(mins(m.t))",
  "cls ? ' ' + cls : ''",
  "coach.busy ? ' disabled' : ''",
  "coach.busy ? 'Thinking&hellip;' : 'What should change?'",
  "coach.rides === null ? '&hellip;' : 'nothing yet'",
  "code.k",
  "combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')",
  "cookTime ? `<span class=\"rtime-badge\">⏱ ${esc(cookTime)}</span>` : ''",
  "ctx",
  "d.cook?' cook':''",
  "d.getDate()",
  "d.h ? ' · ' + (Number(d.h)||0) + ' h' : ''",
  "d.items.length",
  "day.meals.length",
  "dcb",
  "dinnerView === 'all' ? ' class=\"on\"' : ''",
  "dinnerView === 'weekly' ? ' class=\"on\"' : ''",
  "dkc",
  "dn.c ? '$' + (Number(dn.c) || 0).toFixed(2) : ''",
  "esc(S.f)",
  "esc(S.n)",
  "esc(S.z)",
  "esc(Z[i] || ('Zone ' + (i+1)))",
  "esc(a.detail)",
  "esc(a.headline)",
  "esc(aiFood.err)",
  "esc(aiFood.food.n)",
  "esc(aiFood.food.unit.u)",
  "esc(big.name || big.type)",
  "esc(build.name || '')",
  "esc(c.change)",
  "esc(c.id)",
  "esc(c.label)",
  "esc(c.meal)",
  "esc(coach.err)",
  "esc(code.name)",
  "esc(code.why)",
  "esc(cookTime)",
  "esc(d)",
  "esc(d.items.map(i => i.q + ' ' + i.u + ' ' + i.f).join(', '))",
  "esc(d.kind)",
  "esc(d.name || 'Untitled')",
  "esc(d.wd)",
  "esc(day.kind)",
  "esc(day.wd)",
  "esc(dayRide.err)",
  "esc(dn.d)",
  "esc(dn.dish)",
  "esc(dn.tag || 'cook tonight')",
  "esc(f.dir)",
  "esc(f.title)",
  "esc(f.why)",
  "esc(f[0])",
  "esc(foodCase(it.n))",
  "esc(fq)",
  "esc(fq.trim())",
  "esc(hh)",
  "esc(hhm(((tr&&tr.h)||0)))",
  "esc(hhm(S.h))",
  "esc(hhm(secs/3600))",
  "esc(hhm(tr.h))",
  "esc(iso)",
  "esc(it.extraId)",
  "esc(it.f)",
  "esc(it.meal)",
  "esc(it.n)",
  "esc(it.note || 'Pantry staple')",
  "esc(it.note)",
  "esc(it.q || '')",
  "esc(it.q)",
  "esc(it.u)",
  "esc(j)",
  "esc(k)",
  "esc(key)",
  "esc(label)",
  "esc(look.err)",
  "esc(m)",
  "esc(m.category || 'Dinner')",
  "esc(m.err)",
  "esc(m.it)",
  "esc(m.l)",
  "esc(m.me)",
  "esc(m.name)",
  "esc(m.on)",
  "esc(m.serves || '')",
  "esc(m.t)",
  "esc(mName)",
  "esc(ml.l)",
  "esc(ml.t)",
  "esc(n)",
  "esc(name)",
  "esc(note)",
  "esc(p)",
  "esc(plan.block || 'Monthly Block')",
  "esc(pp)",
  "esc(r.serves || '')",
  "esc(recipeSearch)",
  "esc(s)",
  "esc(s.sec)",
  "esc(sec)",
  "esc(selDay)",
  "esc(st)",
  "esc(swap.n)",
  "esc(t)",
  "esc(t.kind || d.kind || 'Ride')",
  "esc(targetItem.q)",
  "esc(tr.kind)",
  "esc(u[0])",
  "esc(w.label || 'Training Schedule')",
  "esc(w.note)",
  "esc(w.week.slice(5))",
  "esc(wk.label || ('Week ' + (idx + 1)))",
  "esc(wk.why)",
  "esc(x)",
  "esc(x.body)",
  "esc(x.key)",
  "esc(x.n)",
  "esc(x.name.replace(' week',''))",
  "esc(x.title)",
  "f > 0 ? 'true' : 'false'",
  "f ? ` <div class=\"ing-macro-card\"> <h4>Nutritional Profile (per 100g)</h4> <div class=\"ing-macro-row\"> <div class=\"ing-macro-col\"><span class=\"ing-macro-lbl\">Energy</span><span class=\"ing-macro-val\">${Math.round(f[2] ? f[2][2] : 0)} kcal</span></div> <div class=\"ing-macro-col\"><span class=\"ing-macro-lbl\">Carb</span><span class=\"ing-macro-val\">${Math.round(f[2] ? f[2][3] : 0)}g</span></div> <div class=\"ing-macro-col\"><span class=\"ing-macro-lbl\">Protein</span><span class=\"ing-macro-val\">${Math.round(f[2] ? f[2][4] : 0)}g</span></div> <div class=\"ing-macro-col\"><span class=\"ing-macro-lbl\">Fat</span><span class=\"ing-macro-val\">${Math.round(f[2] ? f[2][5] : 0)}g</span></div> </div> </div> ` : ''",
  "f.ramp > 0 ? '+' : ''",
  "f.ramp.toFixed(1)",
  "f.tsb > 0 ? '+' : ''",
  "from ? ' <i>from ' + esc(from.d) + '</i>' : ''",
  "gap",
  "gap < 0 ? 'under' : 'over'",
  "gap > 0 ? '+' : ''",
  "gotCount",
  "gph",
  "guess ? ' guess' : ''",
  "guess ? ' planned' : ''",
  "h >= 2 ? 4 : 3",
  "hPct.toFixed(1)",
  "has ? '' : ' off'",
  "heats.length ? ' &middot; reheats ' + esc(heats.join(', ')) : ''",
  "hh",
  "hideChecked ? 'All items checked off!' : 'Your shopping list is empty for this week.'",
  "hrs.toFixed(1)",
  "i",
  "idx",
  "idx === wi ? ' on' : ''",
  "inShop ? `<button class=\"bcan exp-rm-shop\" data-rrmshop=\"${esc(m.name)}\">🗑️ Remove from Shopping List</button>` : `<button class=\"cbtn exp-add-shop\" data-raddshop=\"${esc(m.name)}\">🛒 Add Ingredients to List</button>`",
  "inShop ? `<button class=\"exp-rm-quick\" data-rrmshop=\"${esc(m.name)}\" title=\"Remove ingredients from shopping list\">✓ In List &times;</button>` : `<button class=\"cbtn exp-add-quick\" data-raddshop=\"${esc(m.name)}\">+ List</button>`",
  "inShop ? `<span class=\"rtime-badge rtime-badge-added\">✓ In Shopping List</span>` : ''",
  "infoBtn('ctl')",
  "infoBtn('tsb')",
  "infoBtn('zone')",
  "ings.length ? `<div class=\"rsec\">What you need</div> <ul class=\"ring\">${ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}</li>`).join('')}</ul>` : ''",
  "ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}</li>`).join('')",
  "isHeroQuick ? ' on' : ''",
  "isHeroSinglePan ? ' on' : ''",
  "isNow ? ' today' : ''",
  "isOpen ? ' open' : ''",
  "isOpen ? '▲' : '▼'",
  "isOpen ? ` <div class=\"exp-body\"> ${m.ingredients && m.ingredients.length ? ` <div class=\"rsec\">Ingredients</div> <ul class=\"ring\"> ${m.ingredients.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}</li>`).join('')} </ul> ` : ''} ${m.pantry && m.pantry.length ? ` <div class=\"rsec\">Cupboard &amp; Seasonings</div> <ul class=\"ring pantryring\"> ${m.pantry.map(p => `<li>${esc(p)}</li>`).join('')} </ul> ` : ''} ${m.method && m.method.length ? ` <div class=\"rsec\">Culinary Method</div> <ol class=\"rsteps\"> ${m.method.map(st => `<li>${esc(st)}</li>`).join('')} </ol> ` : ''} <div class=\"exp-actions\"> ${inShop ? `<button class=\"bcan exp-rm-shop\" data-rrmshop=\"${esc(m.name)}\">🗑️ Remove from Shopping List</button>` : `<button class=\"cbtn exp-add-shop\" data-raddshop=\"${esc(m.name)}\">🛒 Add Ingredients to List</button>`} <button class=\"bcan exp-custom\" data-rcustomize=\"${esc(m.name)}\">⚙️ Customize</button> </div> </div> ` : ` <div class=\"exp-preview-actions\"> <button class=\"bcan exp-view-btn\" data-rtoggle=\"${esc(m.name)}\">View Recipe &amp; Method</button> ${inShop ? `<button class=\"exp-rm-quick\" data-rrmshop=\"${esc(m.name)}\" title=\"Remove ingredients from shopping list\">✓ In List &times;</button>` : `<button class=\"cbtn exp-add-quick\" data-raddshop=\"${esc(m.name)}\">+ List</button>`} </div> `",
  "isSel ? ' selected' : ''",
  "isSinglePan ? `<span class=\"rtime-badge rtime-badge-pan\">🍳 1 pan</span>` : ''",
  "isToday ? ' istoday' : ''",
  "isToday ? '<div class=\"todaytag\">Today</div>' : ''",
  "it.c > 0 ? it.c.toFixed(2) : ''",
  "it.c ? '$' + it.c.toFixed(2) + ' &middot; ' : ''",
  "it.extraId ? `<button class=\"del\" data-del=\"${esc(it.extraId)}\" aria-label=\"Remove ${esc(it.n)}\">\\u00d7</button>` : ''",
  "it.meal ? `<span class=\"tagMeal\">🍽️ ${esc(it.meal)}</span>` : ''",
  "it.note ? `<span class=\"note\">${esc(it.note)}</span>`:''",
  "it.pantry === 'out' ? 'Out' : 'Low'",
  "it.pantry ? ` data-pantry-name=\"${esc(it.n)}\"` : ''",
  "it.pantry ? `<span class=\"tagP\">${it.pantry === 'out' ? 'Out' : 'Low'}</span>` : ''",
  "it.q ? `<span class=\"q\">${esc(it.q)}</span>`:''",
  "items",
  "k",
  "k === 'C' ? infoBtn('carbs') : (k === 'F' ? infoBtn('pf') : '')",
  "key === selDay ? ' on' : ''",
  "key === todayKey() ? ' now' : ''",
  "leftCost.toFixed(2)",
  "list.length",
  "list.map(it => { const st = pantryOf(it.n); return `<div class=\"pantry-card\"> <div class=\"pantry-info\"> <span class=\"pantry-name\">${esc(it.n)}</span> <span class=\"pantry-meta\">${it.c ? '$' + it.c.toFixed(2) + ' &middot; ' : ''}${esc(it.note || 'Pantry staple')}</span> </div> <div class=\"pantry-actions\"> <button class=\"pbtn${st === 'ok' ? ' on ok' : ''}\" data-pstate=\"ok\" data-pan-item=\"${esc(it.n)}\">In Stock</button> <button class=\"pbtn${st === 'low' ? ' on low' : ''}\" data-pstate=\"low\" data-pan-item=\"${esc(it.n)}\">Low</button> <button class=\"pbtn${st === 'out' ? ' on out' : ''}\" data-pstate=\"out\" data-pan-item=\"${esc(it.n)}\">Out</button> </div> </div>`; }).join('')",
  "look.busy ? ' disabled' : ''",
  "look.busy ? 'Reading the data&hellip;' : (look.data ? 'Look again' : 'Analyse my training')",
  "lowCount",
  "m.ingredients && m.ingredients.length ? ` <div class=\"rsec\">Ingredients</div> <ul class=\"ring\"> ${m.ingredients.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}</li>`).join('')} </ul> ` : ''",
  "m.ingredients.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}</li>`).join('')",
  "m.method && m.method.length ? ` <div class=\"rsec\">Culinary Method</div> <ol class=\"rsteps\"> ${m.method.map(st => `<li>${esc(st)}</li>`).join('')} </ol> ` : ''",
  "m.method.map(st => `<li>${esc(st)}</li>`).join('')",
  "m.on ? `<i>${esc(m.on)}</i>` : ''",
  "m.pantry && m.pantry.length ? ` <div class=\"rsec\">Cupboard &amp; Seasonings</div> <ul class=\"ring pantryring\"> ${m.pantry.map(p => `<li>${esc(p)}</li>`).join('')} </ul> ` : ''",
  "m.pantry.map(p => `<li>${esc(p)}</li>`).join('')",
  "mark",
  "mealsUsing.length ? ` <div class=\"ing-meals-box\"> <h4>Planned Meals Requiring This</h4> <ul class=\"ing-meals-list\"> ${mealsUsing.map(m => `<li>🍽️ ${esc(m)}</li>`).join('')} </ul> </div> ` : ''",
  "mealsUsing.map(m => `<li>🍽️ ${esc(m)}</li>`).join('')",
  "meas ? '<span class=\"meas\">power meter</span>' : ''",
  "mins(r.cool)",
  "mins(r.warm)",
  "mins(sec)",
  "mins(work)",
  "okCount",
  "on ? ' on' : ''",
  "on?' done':''",
  "onbike ? onbike + 'g fuel' : ''",
  "out",
  "outCount",
  "pPct.toFixed(1)",
  "pct",
  "perHr",
  "perHr ? `<span class=\"pm\">about ${perHr} g carb an hour</span>` : ''",
  "planK",
  "pstate === 'low' ? ' on low' : ''",
  "pstate === 'ok' ? ' on ok' : ''",
  "pstate === 'out' ? ' on out' : ''",
  "pstate.toUpperCase()",
  "r && r.method && r.method.length ? `<div class=\"rsec\">Method</div> <ol class=\"rsteps\">${r.method.map(s => `<li>${esc(s)}</li>`).join('')}</ol> <p class=\"rtip\">${esc(r.serves || '')}</p>` : `<p class=\"rtip\">No method written for this one yet.</p>`",
  "r && r.pantry && r.pantry.length ? `<div class=\"rsec\">From the cupboard</div> <ul class=\"ring pantryring\">${r.pantry.map(x => `<li>${esc(x)}</li>`).join('')}</ul>` : ''",
  "r.method.map(s => `<li>${esc(s)}</li>`).join('')",
  "r.pantry.map(x => `<li>${esc(x)}</li>`).join('')",
  "recipeCategory === c.id ? ' on' : ''",
  "recipeSort === 'carb_desc' ? ' on' : ''",
  "recipeSort === 'kc_desc' ? ' on' : ''",
  "recipeSort === 'name' ? ' on' : ''",
  "recipeSort === 'protein_desc' ? ' on' : ''",
  "recipeSort === 'quick' ? ' on' : ''",
  "ride",
  "rideState",
  "rides",
  "rs.length - 1",
  "rs.length === 2 ? 'ride' : 'rides'",
  "said",
  "sectionsHtml(look.data)",
  "shopView === 'list' ? ' class=\"on\"' : ''",
  "shopView === 'pantry' ? ' class=\"on\"' : ''",
  "sorted.length",
  "sorted.length ? sorted.map(m => renderRecipeCard(m)).join('') : `<div class=\"empty\">No recipes found.<br><br><button class=\"bcan\" id=\"rec_reset_empty\">Show all 100 meals</button></div>`",
  "st",
  "st === 'low' ? ' on low' : ''",
  "st === 'ok' ? ' on ok' : ''",
  "st === 'out' ? ' on out' : ''",
  "st.efficiency_trend_pct",
  "st.efficiency_trend_pct < 0 ? 'dn' : 'up'",
  "st.efficiency_trend_pct > 0 ? '+' : ''",
  "st.efficiency_trend_pct > 0 ? 'up' : 'down'",
  "sum.cb",
  "sum.kc",
  "t",
  "t ? Math.min(100, Math.round(v / t * 100)) : 0",
  "targetItem && targetItem.c ? `<span class=\"ing-badge\">$${targetItem.c.toFixed(2)}</span>` : ''",
  "targetItem && targetItem.q ? `<span class=\"ing-badge\">Qty: ${esc(targetItem.q)}</span>` : ''",
  "targetItem ? esc(targetItem.sec) : 'Pantry Staple'",
  "targetItem.c.toFixed(2)",
  "totalCount",
  "tr && tr.h ? esc(hhm(tr.h)) + ' planned' : 'no riding'",
  "trainView === 'coach' ? ' on' : ''",
  "trainView === 'month' ? ' on' : ''",
  "trainView === 'roadmap' ? ' on' : ''",
  "trainView === 'week' ? ' on' : ''",
  "u",
  "u[2]",
  "v",
  "visibleItems.length",
  "w.hours",
  "w.partial ? ' part' : ''",
  "w.ride_days",
  "w.watts_per_bpm || '&mdash;'",
  "wdIdx >= 0 ? wdIdx + 1 : '?'",
  "wk.name.toLowerCase()",
  "x.days",
  "x.hours"
]
