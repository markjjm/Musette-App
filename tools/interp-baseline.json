[
  "(Math.round(hrs * 100) / 100)",
  "(d.meals||[]).map(ml => `<div class=\"pmeal${/on the bike/i.test(ml.l||'')?' onbike':''}\"> <div class=\"pt\">${esc(ml.t)}</div> <div class=\"px\"><b>${esc(ml.l)}</b>${combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')}<span class=\"pm\">${Number(ml.kc)||0} kcal · ${Number(ml.cb)||0} g carb</span></div> </div>`).join('')",
  "/on the bike/i.test(ml.l||'')?' onbike':''",
  "Number(bk.cb) || 0",
  "Number(bk.kc) || 0",
  "Number(c.kcal_delta) || 0",
  "Number(d.cb)||0",
  "Number(d.d)||0",
  "Number(d.kc)||0",
  "Number(it.c) || 0",
  "Number(it.c)||0",
  "Number(ml.cb)||0",
  "Number(ml.kc)||0",
  "Number(x.d) || 0",
  "WD.map(d => `<div class=\"mhead\">${esc(d)}</div>`).join('')",
  "bk ? `<span class=\"pi\"><i>${Number(bk.cb) || 0}g</i>carb on the bike &middot; ${Number(bk.kc) || 0} kcal</span>` : ''",
  "burn",
  "c.kcal_delta < 0 ? 'dn' : 'up'",
  "c.kcal_delta > 0 ? '+' : ''",
  "cells.join('')",
  "coach.busy ? ' disabled' : ''",
  "coach.busy ? 'Thinking&hellip;' : 'What should change?'",
  "combineMealItems(ml.i).map(it => `<span class=\"pi\"><i>${Number(it.c)||0}</i>${esc(it.n)}</span>`).join('')",
  "cook?' cook':''",
  "d.cook?' cook':''",
  "d.h ? ' · ' + (Number(d.h)||0) + ' h' : ''",
  "dayNum",
  "dn.c ? '$' + (Number(dn.c) || 0).toFixed(2) : ''",
  "esc(a.detail)",
  "esc(a.headline)",
  "esc(c.change)",
  "esc(c.meal)",
  "esc(coach.err)",
  "esc(d)",
  "esc(d.kind)",
  "esc(d.wd)",
  "esc(day.kind)",
  "esc(day.wd)",
  "esc(dn.d)",
  "esc(dn.dish)",
  "esc(dn.tag || 'cook tonight')",
  "esc(hh)",
  "esc(hhm(planH))",
  "esc(hhm(secs / 3600))",
  "esc(it.extraId)",
  "esc(it.n)",
  "esc(it.note)",
  "esc(it.q || '')",
  "esc(it.q)",
  "esc(k)",
  "esc(ml.l)",
  "esc(ml.t)",
  "esc(pp)",
  "esc(r.serves)",
  "esc(s)",
  "esc(s.sec)",
  "esc(shortKind(x.kind))",
  "esc(t.kind || d.kind || 'Ride')",
  "esc(w.note)",
  "esc(weekLabel(w))",
  "esc(x)",
  "from ? ' <i>from ' + esc(from.d) + '</i>' : ''",
  "gap",
  "gap < 0 ? 'under' : 'over'",
  "gap > 0 ? '+' : ''",
  "h20",
  "heats.length ? ' &middot; reheats ' + esc(heats.join(', ')) : ''",
  "hh",
  "hideChecked ? 'All items checked off!' : 'Nothing from ' + (tab==='M'?'Meijer':'Aldi') + ' this week.'",
  "hrs > 0 ? hrs + ' h' : '\\u2013'",
  "i",
  "i===wi?' on':''",
  "ings.length ? `<div class=\"rsec\">What you need</div> <ul class=\"ring\">${ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')}</ul>` : ''",
  "ings.map(it => `<li><i>${esc(it.q || '')}</i>${esc(it.n)}${it.store === 'M' ? '<b>Meijer</b>' : ''}</li>`).join('')",
  "isToday ? ' istoday' : ''",
  "isToday ? '<div class=\"todaytag\">Today</div>' : ''",
  "it.c > 0 ? it.c.toFixed(2) : ''",
  "it.extraId ? `<button class=\"del\" data-del=\"${esc(it.extraId)}\" aria-label=\"Remove ${esc(it.n)}\">\\u00d7</button>` : ''",
  "it.note ? `<span class=\"note\">${esc(it.note)}</span>`:''",
  "it.pantry === 'out' ? 'Out' : 'Low'",
  "it.pantry ? ` data-pantry-name=\"${esc(it.n)}\"` : ''",
  "it.pantry ? `<span class=\"tagP\">${it.pantry === 'out' ? 'Out' : 'Low'}</span>` : ''",
  "it.q ? `<span class=\"q\">${esc(it.q)}</span>`:''",
  "it.store === 'M' ? ' <b class=\"tagM\">Meijer</b>' : ''",
  "it.store === 'M' ? '<b>Meijer</b>' : ''",
  "meas ? ' &middot; <span class=\"meas\">power meter</span>' : ''",
  "on?' done':''",
  "onbike",
  "perHr",
  "perHr ? `<span class=\"pm\">about ${perHr} g carb an hour</span>` : ''",
  "planK",
  "r && r.pantry && r.pantry.length ? `<div class=\"rsec\">From the cupboard</div> <ul class=\"ring pantryring\">${r.pantry.map(x => `<li>${esc(x)}</li>`).join('')}</ul>` : ''",
  "r ? `<div class=\"rsec\">Method</div> <ol class=\"rsteps\">${r.method.map(s => `<li>${esc(s)}</li>`).join('')}</ol> <p class=\"rtip\">${esc(r.serves)}</p>` : `<p class=\"rtip\">No method written for this one yet.</p>`",
  "r.method.map(s => `<li>${esc(s)}</li>`).join('')",
  "r.pantry.map(x => `<li>${esc(x)}</li>`).join('')",
  "ride",
  "rides",
  "rows",
  "s"
]
